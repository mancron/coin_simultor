# 투자손익 패널 테스트 가이드

## 📋 실행 전 체크리스트

### 1. DB 연결 확인
- MySQL 서버가 실행 중인지 확인
- `coin_simulator` 데이터베이스가 생성되어 있는지 확인
- `DBConnection.java`의 연결 정보 확인 (localhost:3306, root, 1234)

### 2. 필요한 테이블 확인
다음 테이블들이 존재해야 합니다:
- `users` - 사용자 정보
- `simulation_sessions` - 세션 정보 (초기 자본금)
- `assets` - 자산 정보
- `orders` - 주문 정보
- `executions` - 체결 정보 (실현손익 포함)

---

## 🚀 실행 방법

### 방법 1: 통합 테스트 클래스 실행 (추천)

```bash
# ProfitLoss_Test.java 실행
# - 자동으로 테스트 데이터 30일치 생성
# - UI 창 자동 실행
```

**실행 순서:**
1. `ProfitLoss_Test.java` 파일을 Eclipse/IntelliJ에서 열기
2. `Run` 버튼 클릭 (또는 Ctrl+F11)
3. 콘솔에 데이터 생성 진행 상황 출력
4. UI 창이 자동으로 열림

---

### 방법 2: 단계별 실행

#### Step 1: 테스트 데이터 생성
```java
// ProfitLossTestData.java의 main 메서드 실행
ProfitLossTestData.generateTestData("user_01", 30);
```

**콘솔 출력 예시:**
```
=== 테스트 데이터 생성 시작 ===
사용자: user_01
기간: 30일
테스트 사용자 생성 완료: user_01
..........10일 완료
..........20일 완료
..........30일 완료
=== 테스트 데이터 생성 완료 ===
```

#### Step 2: UI 실행
```java
// ProfitLoss_MainPanel.java의 main 메서드 실행
public static void main(String[] args) {
    SwingUtilities.invokeLater(() -> {
        JFrame frame = new JFrame("투자손익 테스트");
        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        frame.setSize(1000, 700);
        frame.setLocationRelativeTo(null);

        ProfitLoss_MainPanel panel = new ProfitLoss_MainPanel("user_01");
        frame.add(panel);
        frame.setVisible(true);
    });
}
```

---

## 🎯 예상 결과

### UI 구성
```
┌─────────────────────────────────────────────────┐
│  [요약 통계]                                     │
│  기간 누적 손익: +1,234,567 KRW                  │
│  기간 누적 수익률: +1.23%                        │
│  기간 평균 투자금액: 101,234,567 KRW             │
├─────────────────────────────────────────────────┤
│  [차트 영역]                                     │
│  누적 수익률 라인차트  │  일별 손익 막대차트     │
├─────────────────────────────────────────────────┤
│  [상세 테이블]                                   │
│  일자 │ 일일손익 │ 일일수익률 │ 누적손익 │ ...   │
│  02.12│ +50,000 │ +0.05%    │ +1.2M   │ ...   │
│  02.11│ -20,000 │ -0.02%    │ +1.15M  │ ...   │
└─────────────────────────────────────────────────┘
```

---

## ⚠️ 문제 해결

### 1. "No suitable driver found" 에러
```
해결: pom.xml에 MySQL 드라이버 확인
<dependency>
    <groupId>mysql</groupId>
    <artifactId>mysql-connector-java</artifactId>
    <version>8.0.33</version>
</dependency>
```

### 2. "Table doesn't exist" 에러
```
해결: CreateDatabase.java 실행하여 테이블 생성
```

### 3. 데이터가 표시되지 않음
```
해결 1: ProfitLossTestData.java 먼저 실행
해결 2: DB에서 직접 확인
  SELECT COUNT(*) FROM executions;
```

### 4. HikariCP 연결 에러
```
해결: DBConnection.java의 연결 정보 확인
- URL: jdbc:mysql://localhost:3306/coin_simulator
- Username: root
- Password: 1234
```

---

## 🔧 데이터 초기화

테스트 데이터를 삭제하고 다시 생성하려면:

```sql
-- executions 테이블 데이터 삭제
DELETE FROM executions WHERE order_id IN 
  (SELECT order_id FROM orders WHERE user_id = 'user_01');

-- orders 테이블 데이터 삭제  
DELETE FROM orders WHERE user_id = 'user_01';

-- 사용자 삭제 (필요 시)
DELETE FROM users WHERE user_id = 'user_01';
```

---

## 📊 테스트 데이터 구조

생성되는 데이터:
- **사용자:** user_01
- **초기 자본:** 100,000,000원
- **거래 기간:** 30일
- **일별 거래:** 3~7건
- **거래 유형:** 매수 + 매도 (랜덤 손익 발생)

---

## 💡 추가 기능 테스트

### 기간 변경 테스트
```java
// 7일치만 조회
panel.refresh(7);

// 90일치 조회
panel.refresh(90);
```

### 다른 사용자 테스트
```java
// user_02 데이터 생성
ProfitLossTestData.generateTestData("user_02", 30);

// user_02 UI 실행
new ProfitLoss_MainPanel("user_02");
```
