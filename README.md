

# 가상화폐 모의투자 시스템 (Coin Simulator)

팀원 공용 자바 데스크톱 애플리케이션 프로젝트입니다.
본 문서는 각 패키지의 역할과 개발 가이드를 포함합니다.

## 🛠 개발 환경 (Environment)

* **Java:** JDK 17
* **Build:** Maven
* **Database:** MySQL 8.0 (공용 DB 사용)
* **GUI:** Java Swing

## 패키지 구조 및 역할 가이드 (Package Structure)

소스 코드는 `src/main/java/com/team/coin_simulator` 내부에 위치합니다.

### 1. `config` (설정)

* **역할:** DB 연결 설정 및 전역 상수를 관리합니다.
* **구현 목록:**
* `DBConfig.java`: **[싱글톤]** 공용 DB 접속 정보를 관리하고 `Connection` 객체를 반환합니다.
* *주의:* 공용 DB 계정/비밀번호가 포함되므로 외부 유출에 주의하세요.



### 2. `model` (데이터 객체 / DTO)

* **역할:** 데이터를 담아 나르는 바구니 역할입니다. 로직을 포함하지 않고 변수와 Getter/Setter만 가집니다.
* **구현 목록:**
* `User.java`: 사용자 정보 (id, password, nickname...)
* `Asset.java`: 보유 자산 정보 (currency, balance, avg_price...)
* `Order.java`: 주문 정보 (order_id, price, volume, type...)
* `Execution.java`: 체결 및 실현 손익 정보 (pnl, roi 포함)
* `Candle.java`: 차트용 캔들 데이터 (open, high, low, close...)



### 3. `dao` (데이터 접근 / DAO)

* **역할:** 데이터베이스(MySQL)에 직접 SQL을 날리는 클래스들입니다.
* **구현 목록:**
* `UserDAO.java`: 로그인(`SELECT`), 회원가입(`INSERT`) 처리.
* `OrderDAO.java`: 주문 접수(`INSERT`), 미체결 주문 조회(`SELECT`).
* `AssetDAO.java`: 내 자산 조회, 잔고 업데이트(`UPDATE`).
* `HistoryDAO.java`: 투자 내역 및 실현 손익 조회.
* *규칙:* 모든 메서드는 `try-with-resources`를 사용하여 DB 연결을 확실히 닫아야 합니다.



### 4. `service` (핵심 로직 / 계산기)

* **역할:** UI와 DB 사이에서 복잡한 계산이나 게임 로직을 수행합니다.
* **구현 목록:**
* `MatchingEngine.java`: **[핵심]** 매수/매도 주문이 체결되는지 확인하는 엔진.
* `VirtualClock.java`: 모의투자 시간(배속 조절)을 관리하는 타이머.
* `AssetCalculator.java`: 주문 가능 수량 계산(수수료 포함), 수익률 계산 로직.
* `OrderBookService.java`: 현재가 기준으로 가상 호가창 데이터를 생성.



### 5. `ui` (화면 / GUI)

* **역할:** Java Swing을 이용해 사용자가 보는 화면을 그립니다.
* **구현 목록:**
* `LoginFrame.java`: 프로그램 시작 시 뜨는 로그인/회원가입 창.
* `MainFrame.java`: 로그인 후 뜨는 메인 대시보드.
* **`panel` (패키지 분리 권장):**
* `ChartPanel.java`: 캔들 차트 그리기.
* `OrderPanel.java`: 매수/매도 입력 및 버튼.
* `OrderBookPanel.java`: 호가창 리스트.
* `HistoryPanel.java`: 보유자산 및 투자내역 테이블.





### 6. `util` (도구)

* **역할:** 프로젝트 전반에서 쓰이는 편리한 도구 모음.
* **구현 목록:**
* `SecurityUtil.java`: 비밀번호 SHA-256 암호화.
* `NumberFormatter.java`: 금액 콤마(,) 표기 및 소수점 처리.



---

## 개발 시 주의사항 (Rules)

1. **DB 연결 해제 필수:**
* `DBConfig.getInstance().getConnection()` 사용 후에는 반드시 `close()` 하거나 `try-with-resources` 문법을 사용해 연결을 끊어야 합니다. 안 그러면 DB가 멈춥니다.


2. **UI와 로직 분리:**
* `JFrame`이나 `JPanel` 안에서 복잡한 SQL 쿼리를 직접 짜지 마세요. 반드시 `DAO`를 호출해서 데이터만 받아오세요.


3. **Maven 갱신:**
* `pom.xml`에 라이브러리를 추가했다면 팀원들에게 알리고, 팀원들은 프로젝트 우클릭 -> Maven -> **Reload Project**를 꼭 해야 합니다.



---

## 시작 방법 (Getting Started)

1. 이 프로젝트를 `Clone` 또는 다운로드합니다.
2. IDE(IntelliJ/Eclipse)에서 `pom.xml`을 엽니다.
3. Maven 의존성을 다운로드(Reload) 합니다.
4. `src/main/java/com/team/coin_simulator/App.java`를 실행(Run)합니다.
